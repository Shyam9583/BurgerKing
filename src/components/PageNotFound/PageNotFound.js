import React from "react";

const pageNotFound = (props) => (
  <h1 style={{ textAlign: "center", top: 50 }}>
    <span style={{ color: "red", fontWeight: "bold" }}>ERROR 404</span>
    <br />
    Page not found!
  </h1>
);

export default pageNotFound;
